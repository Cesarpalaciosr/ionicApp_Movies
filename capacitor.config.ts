import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Peliculas_Cesar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
